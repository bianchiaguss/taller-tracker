# Base de datos
DATABASE_URL=postgresql://taller_user:taller_pass@db:5432/taller_db

# Seguridad / JWT
SECRET_KEY=cambiar-esto-por-una-clave-larga-y-aleatoria
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=480

# CORS - dominio del frontend
FRONTEND_URL=http://localhost:5173

# Almacenamiento de imagenes (MVP: local). Para produccion, completar con Cloudinary
STORAGE_BACKEND=local
UPLOADS_DIR=uploads
CLOUDINARY_URL=

# Email (Gmail SMTP). Sin SMTP_PASSWORD el sistema solo loguea (no envia).
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_EMAIL=notificaciones.bianchi@gmail.com
SMTP_PASSWORD=
EMAIL_FROM=Bianchi Detailing <notificaciones.bianchi@gmail.com>
